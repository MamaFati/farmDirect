# Django Secret Key (secure random key for production)
SECRET_KEY=&!b2u^&#l2sw%ly*k6908n_=zoldz0$m$eikj$8_o=p@rqc)or

# Debug mode (set to False in production for security)
DEBUG=True

# Database selection (set one to True, the other to False)
USE_SQLITE=True
USE_POSTGRES=False

# Allowed hosts (comma-separated list of domains/IPs; use '*' for development)
ALLOWED_HOSTS=localhost,127.0.0.1

# Database configuration (PostgreSQL example; used if USE_POSTGRES=True)
DATABASE_URL=postgresql://username:password@localhost:5432/dbname
DB_NAME=dbname
DB_PORT=5432
DB_PASSWORD=password
DB_HOST=localhost
DB_USER=username

# Database configuration (SQLite; used if USE_SQLITE=True)
SQLITE_DB_PATH=db.sqlite3

# Email configuration (SMTP example for Gmail)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-email-password
EMAIL_USE_TLS=True

# Static and media file storage (optional, e.g., for AWS S3)
USE_AWS=False
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_STORAGE_BUCKET_NAME=your-bucket-name
AWS_S3_REGION_NAME=us-east-1

# CORS configuration (comma-separated list of allowed origins)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://127.0.0.1:9000,http://127.0.0.1:8000
CORS_ALLOW_CREDENTIALS=True
CORS_ALLOW_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_ALLOW_HEADERS=content-type,authorization

# Logging configuration
LOG_FILE_PATH=logs/app.log
DJANGO_LOG_LEVEL=INFO
DB_LOG_LEVEL=INFO
APP_LOG_LEVEL=DEBUG

# Swagger configuration
SWAGGER_API_URL=http://127.0.0.1:9000

# API Versioning configuration
API_DEFAULT_VERSION=v1
API_ALLOWED_VERSIONS=v1

# Other optional settings
DJANGO_ENV=development